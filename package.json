{
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "homepage": "http://{huibinkim}.github.io/{canvas}"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
